import React from 'react';
// component
import HeroScreen from '../components/ui/HeroScreen';

const Home = () => {
    return (
        <div>
            <HeroScreen/>
        </div>
    );
};

export default Home;